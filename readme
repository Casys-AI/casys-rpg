Extract les sections
 -> vérifier les sections car des fois il voit mal les chemins, mais si on 
 fait un agent qui extrait au fur et a mesure les chemins alors ça ira

Il faut modifier le script d'extraction pour générer instuctions
Run l extract metadata

run l'extract des images